// enemy-related functions
class enemyMan {
    constructor(scene) {
        this.scene = scene; // scene that the enemy manager is working on
        this.enemyCount = 0; // total number of enemies in current scene
    }

    static placeEnemies(scene = this.scene){

    }
}
